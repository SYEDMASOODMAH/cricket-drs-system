# identity-access

**Status:** scaffold only (Phase 1) — no domain logic implemented yet.

See `/docs/architecture.md` for this service's responsibilities and
`/docs/phases.md` for when its features land.

## Run locally

```
go run ./cmd
```

Health check: `GET http://localhost:8080/healthz`

## Test

```
go test ./...
```
