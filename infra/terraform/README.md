# terraform

Cloud-agnostic infra-as-code (docs/architecture.md Section 14). No modules implemented yet —
each is added when the corresponding phase (docs/phases.md) needs it, to avoid provisioning
unused infrastructure ahead of need.
