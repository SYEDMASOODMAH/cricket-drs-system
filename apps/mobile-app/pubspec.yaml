name: cricket_drs_mobile
description: Cricket DRS mobile app — player/coach/fan companion (see docs/design.md Section 12)
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
