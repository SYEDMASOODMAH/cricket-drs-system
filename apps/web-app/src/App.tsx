// Root component — scaffold only (Phase 1). Real dashboard views land in
// Phase 7/9 per docs/phases.md, following the layout in docs/design.md
// Section 8 (persistent left-nav + KPI card grid).
export default function App() {
  return <div>Cricket DRS — Web App (scaffold)</div>;
}
