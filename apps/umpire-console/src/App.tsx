// Root component — scaffold only (Phase 1). The real review screen (single-
// focus layout: match context / trajectory evidence / confidence + confirm-
// override actions) lands in Phase 7 per docs/phases.md, following the
// design in docs/design.md Section 9.
export default function App() {
  return <div>Cricket DRS — Umpire Review Console (scaffold)</div>;
}
