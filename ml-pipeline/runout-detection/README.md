# runout-detection

**Status:** scaffold only (Phase 0/3+ per docs/phases.md — see that doc for exact phase).

See `/docs/architecture.md` Section 8/9 for this component's role in the CV/ML pipeline.

## Setup

```
cd ml-pipeline
pip install -e ".[dev]"
```

## Test

```
pytest runout-detection/tests
```
